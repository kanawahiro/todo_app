# 見積もり時間機能追加 - 実装計画書

## 📋 概要

タスクに「見積もり時間」フィールドを追加し、タスク実行前にかかる時間を予測・記録できるようにします。

---

## 🎯 要件サマリー

### 主要機能
1. **見積もり時間の入力**: タスク登録時・編集時に見積もり時間を入力可能
2. **分単位の数値入力**: 半角数字で自由に入力（例: 5, 10, 15, 30, 60）
3. **表示箇所**: タスク名・メモの下に見積もり時間を表示
4. **適用範囲**:
   - 今日タブのタスクカード
   - 登録タブのAI抽出フォーム
   - 手動タスク追加時

---

## 📊 データ構造

### タスクオブジェクトへの追加

既存のタスク構造に `estimatedMinutes` プロパティを追加:

```javascript
{
  id: number,
  name: string,
  memo: string,
  tag: string,
  status: string,
  statusComment: string,
  registeredDate: string,
  workDates: string[],
  completedDate: string | null,
  accumulatedTime: number,           // 実績時間（秒）
  startedAt: number | null,
  workSessions: array,
  order: number,
  estimatedMinutes: number           // ★NEW★ 見積もり時間（分）
}
```

**例:**
```javascript
{
  id: 1736668800123.456,
  name: "相場分析",
  memo: "朝イチで市場動向をチェック",
  estimatedMinutes: 30,               // 30分の見積もり
  tag: "仕事",
  status: "未着手",
  accumulatedTime: 0,
  // ...その他のプロパティ
}
```

---

## 🎨 UI設計

### 1. 今日タブ - TaskCard（タスクカード）

```
┌─────────────────────────────────────────────────────────┐
│ [📍] [タスク名を入力...________________] [↑][↓]        │
│                                                          │
│ [📝 メモ_______________________________________]        │
│                                                          │
│ ⏱️ 見積: [__30__] 分  ← ★NEW★                         │
│                                                          │
│ 📅 登録: 2026/01/12                                     │
│ 未着手                           ⏱️ 0:00:00            │
│ [コメント_____________________________________]         │
│                                                          │
│ [▶ 開始] [🗑]                                           │
└─────────────────────────────────────────────────────────┘
```

**配置場所**: メモの下、登録日の上

### 2. 登録タブ - AI抽出後の編集画面

```
┌─────────────────────────────────────────────────────────┐
│ 抽出タスク:                                              │
├─────────────────────────────────────────────────────────┤
│ ┌───────────────────────────────────────────────────┐   │
│ │ [☑] [相場分析_______________________]             │   │
│ │     [メモ: 朝イチで市場動向をチェック__________]   │   │
│ │     見積: [__30__] 分  [タグ: 仕事 ▼]            │   │ ← ★NEW★
│ └───────────────────────────────────────────────────┘   │
│ ┌───────────────────────────────────────────────────┐   │
│ │ [☑] [メール配信_____________________]             │   │
│ │     [メモ: 週次レポート配信___________________]   │   │
│ │     見積: [__60__] 分  [タグ: 仕事 ▼]            │   │ ← ★NEW★
│ └───────────────────────────────────────────────────┘   │
│                                                          │
│ [登録する] [☑を削除]                                    │
└─────────────────────────────────────────────────────────┘
```

**配置場所**: メモとタグの間

### 3. 手動タスク追加フォーム（タグカラム内）

現在のフォーム:
```
[タスク名を入力...]
```

変更後:
```
[タスク名を入力...]
[メモ（任意）____________]
見積: [___] 分  ← ★NEW★
```

---

## 🔧 実装の詳細

### 変更・追加ファイル一覧

| ファイル | 変更内容 | 優先度 |
|---------|---------|-------|
| `src/App.jsx` | データマイグレーション、タスク登録時のestimatedMinutes初期化 | 🔴 高 |
| `src/components/TaskCard.jsx` | 見積もり時間入力欄の追加 | 🔴 高 |
| `src/components/AppTabs.jsx` | AI抽出後の編集画面に見積もり時間入力欄追加 | 🔴 高 |
| `src/components/TagColumn.jsx` | 手動タスク追加時の見積もり時間入力 | 🟡 中 |

---

## 🛠️ 実装ステップ

### Phase 1: データ層（App.jsx）

#### 1-1. データマイグレーション

既存タスクに `estimatedMinutes` プロパティを追加（デフォルト0）:

```javascript
// App.jsx
useEffect(() => {
  const migrateData = async () => {
    const savedTasks = await window.storage.get('tasks');
    if (savedTasks) {
      const parsedTasks = JSON.parse(savedTasks);

      // estimatedMinutesが存在しないタスクにデフォルト値0を設定
      const migratedTasks = parsedTasks.map(task => ({
        ...task,
        estimatedMinutes: task.estimatedMinutes ?? 0
      }));

      setTasks(migratedTasks);

      // マイグレーション済みデータを保存
      await window.storage.set('tasks', JSON.stringify(migratedTasks));
    }
  };

  migrateData();
}, []);
```

**重要**:
- 初回マウント時に1回だけ実行
- `??` 演算子で `undefined` と `null` のみデフォルト値を設定
- `0` は有効な値として保持

#### 1-2. タスク登録時の初期化

**AI抽出後の登録処理** (`registerTasks` 関数):

```javascript
// App.jsx - registerTasks関数内
const newTasks = extracted
  .filter(t => !toDelete.has(t.tid))
  .map((t, index) => {
    // ...既存のコード
    return {
      id: newId,
      name: t.name,
      memo: t.memo,
      tag: t.tag,
      estimatedMinutes: t.estimatedMinutes || 0,  // ★NEW★
      status: '未着手',
      // ...その他のプロパティ
    };
  });
```

**手動タスク追加処理** (`addManualTask` 関数):

```javascript
// App.jsx - addManualTask関数
const addManualTask = useCallback((tag, name = '', memo = '', estimatedMinutes = 0) => {
  const today = new Date().toISOString().split('T')[0];
  const tagTasks = tasks.filter(t => t.tag === tag);
  const minOrder = tagTasks.length > 0
    ? Math.min(...tagTasks.map(t => t.order || 0))
    : 0;

  const newTask = {
    id: Date.now() + Math.random(),
    name,
    memo,
    tag,
    estimatedMinutes,  // ★NEW★ パラメータから受け取る
    status: '未着手',
    statusComment: '',
    registeredDate: today,
    workDates: [],
    completedDate: null,
    accumulatedTime: 0,
    startedAt: null,
    workSessions: [],
    order: minOrder - 1
  };

  setTasks(prev => [...prev, newTask]);
  setNewTaskId(newTask.id);
}, [tasks]);
```

---

### Phase 2: UI層（コンポーネント）

#### 2-1. TaskCard.jsx - タスクカードへの追加

**配置場所**: メモ入力欄の下、登録日の上

```javascript
// TaskCard.jsx
export const TaskCard = memo(function TaskCard({
  task,
  // ...既存のprops
}) {
  // ...既存のコード

  return (
    <div style={{ ...styles.card }}>
      {/* タスク名 */}
      <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '8px' }}>
        <span>{getStatusIcon(task.status)}</span>
        <input
          type="text"
          value={task.name}
          onChange={(e) => onUpdateTask(task.id, 'name', e.target.value)}
          placeholder="タスク名を入力..."
          style={{ ...styles.input, flex: 1, padding: '6px 10px' }}
        />
        {/* 上下ボタン */}
      </div>

      {/* メモ */}
      <input
        type="text"
        value={task.memo || ''}
        onChange={(e) => onUpdateTask(task.id, 'memo', e.target.value)}
        placeholder="📝 メモ"
        style={{ ...styles.input, marginBottom: '8px', padding: '6px 10px', fontSize: '0.85rem' }}
      />

      {/* ★NEW★ 見積もり時間 */}
      <div style={{
        display: 'flex',
        alignItems: 'center',
        gap: '8px',
        marginBottom: '8px',
        fontSize: '0.85rem'
      }}>
        <label style={{ color: '#6b6b6b', display: 'flex', alignItems: 'center', gap: '4px' }}>
          ⏱️ 見積:
        </label>
        <input
          type="number"
          value={task.estimatedMinutes || ''}
          onChange={(e) => onUpdateTask(task.id, 'estimatedMinutes', Number(e.target.value) || 0)}
          placeholder="0"
          min="0"
          step="5"
          style={{
            ...styles.input,
            width: '60px',
            padding: '4px 8px',
            textAlign: 'right'
          }}
        />
        <span style={{ color: '#6b6b6b' }}>分</span>
      </div>

      {/* 登録日 */}
      <div style={{ fontSize: '0.8rem', color: '#6b6b6b', marginBottom: '8px' }}>
        📅 登録: {formatDateShort(task.registeredDate)}
      </div>

      {/* ステータスと実績時間 */}
      <div style={{ display: 'flex', justifyContent: 'space-between', fontSize: '0.85rem', marginBottom: '8px' }}>
        <span style={{ color: statusColor }}>{task.status}</span>
        <span style={{ color: '#6b6b6b' }}>⏱️ {formatTime(elapsedTime)}</span>
      </div>

      {/* 以降は既存のコード */}
      {/* ... */}
    </div>
  );
});
```

**スタイリングのポイント**:
- 数値入力欄の幅: `60px`（2〜3桁に最適）
- テキスト揃え: `right`（数値は右揃えが見やすい）
- step属性: `5`（5分刻みで増減しやすい）
- min属性: `0`（負の値を防止）

---

#### 2-2. AppTabs.jsx - AI抽出後の編集画面

**RegisterTab コンポーネント内**:

```javascript
// AppTabs.jsx - RegisterTab
export function RegisterTab({
  input,
  setInput,
  extracting,
  extractTasks,
  extractError,
  extracted,
  toDelete,
  setToDelete,
  setExtracted,
  registerTasks,
  // ...その他のprops
}) {
  return (
    <div>
      {/* タスク入力・抽出UI（既存） */}
      {/* ... */}

      {/* 抽出タスク編集画面 */}
      {extracted.length > 0 && (
        <div style={{ marginTop: '24px' }}>
          <h3>抽出タスク:</h3>
          {extracted.map(task => (
            <div
              key={task.tid}
              style={{
                background: 'rgba(255,255,255,0.05)',
                borderRadius: '8px',
                padding: '12px',
                marginBottom: '8px'
              }}
            >
              {/* チェックボックスとタスク名 */}
              <div style={{ display: 'flex', alignItems: 'center', gap: '10px', marginBottom: '8px' }}>
                <input
                  type="checkbox"
                  checked={toDelete.has(task.tid)}
                  onChange={(e) => {
                    const s = new Set(toDelete);
                    e.target.checked ? s.add(task.tid) : s.delete(task.tid);
                    setToDelete(s);
                  }}
                />
                <input
                  type="text"
                  value={task.name}
                  onChange={(e) => setExtracted(prev =>
                    prev.map(t => t.tid === task.tid ? { ...t, name: e.target.value } : t)
                  )}
                  style={{ ...styles.input, flex: 1 }}
                />
              </div>

              {/* メモ、見積もり時間、タグ */}
              <div style={{ display: 'flex', gap: '8px', marginLeft: '26px', flexWrap: 'wrap' }}>
                {/* メモ */}
                <input
                  type="text"
                  value={task.memo}
                  onChange={(e) => setExtracted(prev =>
                    prev.map(t => t.tid === task.tid ? { ...t, memo: e.target.value } : t)
                  )}
                  placeholder="メモ"
                  style={{ ...styles.input, flex: 1, minWidth: '150px' }}
                />

                {/* ★NEW★ 見積もり時間 */}
                <div style={{ display: 'flex', alignItems: 'center', gap: '4px' }}>
                  <label style={{ fontSize: '0.85rem', color: '#888' }}>見積:</label>
                  <input
                    type="number"
                    value={task.estimatedMinutes || ''}
                    onChange={(e) => setExtracted(prev =>
                      prev.map(t => t.tid === task.tid
                        ? { ...t, estimatedMinutes: Number(e.target.value) || 0 }
                        : t)
                    )}
                    placeholder="0"
                    min="0"
                    step="5"
                    style={{
                      ...styles.input,
                      width: '60px',
                      textAlign: 'right'
                    }}
                  />
                  <span style={{ fontSize: '0.85rem', color: '#888' }}>分</span>
                </div>

                {/* タグ */}
                <select
                  value={task.tag}
                  onChange={(e) => setExtracted(prev =>
                    prev.map(t => t.tid === task.tid ? { ...t, tag: e.target.value } : t)
                  )}
                  style={{ ...styles.input, width: 'auto', minWidth: '100px' }}
                >
                  <option value="">タグなし</option>
                  {tags.map(tg => (
                    <option key={tg} value={tg}>{tg}</option>
                  ))}
                </select>
              </div>
            </div>
          ))}

          {/* 登録ボタン（既存） */}
          <div style={{ display: 'flex', gap: '12px', marginTop: '12px' }}>
            <button style={styles.btn} onClick={registerTasks}>登録する</button>
            {/* ... */}
          </div>
        </div>
      )}

      {/* タグ管理（既存） */}
      {/* ... */}
    </div>
  );
}
```

**データ初期化**:

AI抽出時に `estimatedMinutes` プロパティを初期化:

```javascript
// App.jsx - extractTasks関数内
const extractTasks = useCallback(async () => {
  // ...既存のコード

  try {
    const response = await fetch('/api/extract-tasks', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ input, tags })
    });

    const data = await response.json();

    if (data.success) {
      const tasksWithId = data.tasks.map((t, idx) => ({
        ...t,
        tid: Date.now() + idx,
        estimatedMinutes: 0  // ★NEW★ デフォルト0で初期化
      }));
      setExtracted(tasksWithId);
      setAiStatus('ok');
    }
  } catch (error) {
    // エラー処理
    // フォールバック時も estimatedMinutes を追加
    const fallbackTasks = lines.map((line, idx) => ({
      name: line,
      memo: '',
      tag: '',
      tid: Date.now() + idx,
      estimatedMinutes: 0  // ★NEW★
    }));
    setExtracted(fallbackTasks);
    setAiStatus('ng');
  }
}, [input, tags]);
```

---

#### 2-3. TagColumn.jsx - 手動タスク追加フォーム

**現在の実装**: タグカラムのヘッダーに「+」ボタンのみ

**変更案**: `onAddTask` コールバックに見積もり時間パラメータを追加

```javascript
// TagColumn.jsx - ヘッダー部分
<button
  onClick={() => onAddTask(tag)}  // 既存: パラメータなし
  style={{
    background: '#2563eb',
    border: 'none',
    color: '#fff',
    padding: '4px 10px',
    borderRadius: '4px',
    cursor: 'pointer',
    fontWeight: 'bold'
  }}
>+</button>
```

**選択肢1**: 簡易実装（推奨）
- 「+」ボタンクリックで即座にタスク作成（見積もり時間は0、後で編集）
- シンプルで既存の動作を維持

**選択肢2**: フォーム実装
- 「+」ボタンでフォームを表示
- タスク名・メモ・見積もり時間を入力してから作成
- より丁寧だがUI複雑化

**推奨**: 選択肢1（簡易実装）
理由:
- タスク作成後すぐに TaskCard で編集可能
- UIがシンプルで使いやすい
- 既存の動作フローを変更しない

---

## 📁 データ保存

### LocalStorage構成（変更なし）

| キー | 値 | 備考 |
|------|---|------|
| `tasks` | タスク配列（JSON） | `estimatedMinutes`プロパティ追加 |
| `tags` | タグ配列（JSON） | 既存 |
| `tagOrder` | タグ順序配列（JSON） | 既存 |

---

## ✅ 検証・テスト項目

### データマイグレーション
- [ ] 既存タスクに `estimatedMinutes: 0` が自動追加される
- [ ] 既にデータを持つタスクは上書きされない
- [ ] ページリロード後もデータが保持される

### UI - TaskCard
- [ ] 見積もり時間の入力欄が表示される
- [ ] 数値入力が正しく動作する
- [ ] 0分の場合も正しく保存される
- [ ] 負の値は入力できない（min属性）
- [ ] step属性で5分刻みの増減が動作する

### UI - 登録タブ（AI抽出）
- [ ] 抽出タスクに見積もり時間入力欄が表示される
- [ ] 見積もり時間を編集できる
- [ ] 登録時に見積もり時間が保存される
- [ ] デフォルト値が0になる

### UI - 手動タスク追加
- [ ] 「+」ボタンでタスクが作成される
- [ ] 作成後にTaskCardで見積もり時間を編集できる

### エッジケース
- [ ] 見積もり時間が空欄の場合、0として扱われる
- [ ] 小数点入力時の動作（整数に丸める）
- [ ] 極端に大きい数値（例: 10000分）の入力

---

## 🎨 スタイル定義（参考）

```javascript
// styles.jsに追加（必要に応じて）
export const styles = {
  // ...既存スタイル

  estimatedTimeInput: {
    width: '60px',
    padding: '4px 8px',
    textAlign: 'right',
    fontSize: '0.85rem',
    border: '1px solid #e5e7eb',
    borderRadius: '4px',
    outline: 'none'
  },

  estimatedTimeLabel: {
    fontSize: '0.85rem',
    color: '#6b6b6b',
    display: 'flex',
    alignItems: 'center',
    gap: '4px'
  }
};
```

---

## 🚀 実装優先順位

### Phase 1: データ基盤（必須）
1. **データマイグレーション** - 既存タスクへのプロパティ追加
2. **タスク登録処理の更新** - AI抽出・手動追加時の初期化

### Phase 2: 基本UI（必須）
1. **TaskCard** - タスクカードへの見積もり時間入力欄追加
2. **登録タブ** - AI抽出後の編集画面への入力欄追加

### Phase 3: 拡張機能（オプション）
1. **手動タスク追加** - フォーム追加（または簡易実装）
2. **バリデーション強化** - 入力値の検証
3. **見積vs実績の比較表示** - 将来的な拡張

---

## 📝 補足事項

### 見積もり時間の単位（分）を選んだ理由
- **使いやすさ**: 日常的なタスクは分単位で見積もることが多い
- **精度**: 秒単位は細かすぎ、時間単位は粗すぎる
- **入力の簡潔性**: 「30」と入力するだけ（「0.5時間」より直感的）

### 将来的な拡張機能
1. **見積vs実績の比較**
   - タスク完了時に「見積: 30分 / 実績: 45分」のように表示
   - 乖離率の計算とフィードバック

2. **見積もり精度の分析**
   - 振り返りタブで「見積もり精度」を表示
   - タグ別の平均乖離率

3. **時間帯の提案**
   - 見積もり時間から作業時間帯を提案
   - カレンダービューとの連携

4. **プリセット値**
   - よく使う見積もり時間をボタン化（15分、30分、60分など）
   - クイック入力機能

---

## 🔄 データマイグレーション戦略

### マイグレーションタイミング
- アプリ起動時（初回マウント時）に1回だけ実行
- `useEffect` の依存配列を空にして初回のみ実行

### マイグレーションロジック
```javascript
const migrateTaskData = (tasks) => {
  return tasks.map(task => {
    // estimatedMinutesが存在しない場合のみデフォルト値0を設定
    if (task.estimatedMinutes === undefined) {
      return { ...task, estimatedMinutes: 0 };
    }
    return task;
  });
};
```

### ロールバック戦略
- マイグレーション前のデータをバックアップ不要
- `estimatedMinutes` プロパティの追加のみで破壊的変更なし
- 既存データの互換性を維持

---

## 📸 完成イメージ（モックアップ）

### タスクカード表示
```
┌─────────────────────────────────────────────────────────┐
│ 📍 相場分析                                        [↑][↓]│
│                                                          │
│ 📝 朝イチで市場動向をチェック                            │
│                                                          │
│ ⏱️ 見積: [__30__] 分                                   │
│                                                          │
│ 📅 登録: 2026/01/12                                     │
│ 未着手                                    ⏱️ 0:00:00   │
│ [コメント_____________________________________]         │
│                                                          │
│ [▶ 開始] [🗑]                                           │
└─────────────────────────────────────────────────────────┘
```

### AI抽出後の編集画面
```
┌─────────────────────────────────────────────────────────┐
│ 抽出タスク:                                [AI処理OK]    │
├─────────────────────────────────────────────────────────┤
│ [☑] 相場分析                                             │
│     メモ: 朝イチで市場動向をチェック                     │
│     見積: [__30__] 分  タグ: [仕事 ▼]                  │
├─────────────────────────────────────────────────────────┤
│ [☑] メール配信                                           │
│     メモ: 週次レポート配信                               │
│     見積: [__60__] 分  タグ: [仕事 ▼]                  │
├─────────────────────────────────────────────────────────┤
│ [ ] 日報作成                                             │
│     メモ: 業務終了時に作成                               │
│     見積: [__15__] 分  タグ: [なし ▼]                  │
└─────────────────────────────────────────────────────────┘
```

---

以上が見積もり時間機能の実装計画書です。
